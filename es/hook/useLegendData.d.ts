import { UseLegendProps } from '../types';
export declare const useLegendData: ({ group, dataSource, position, chartType, }: Pick<UseLegendProps, 'group' | 'dataSource' | 'position' | 'chartType'>) => (string | number)[];
